apiVersion: v1
kind: Pod
metadata:
  name: nginx-basics
  namespace: session9-demo
  labels:
    app: nginx-basics
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
